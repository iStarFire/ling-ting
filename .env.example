# 复制为 .env 并按本机实际 JDK 17 路径修改，随后 ./gradlew 会自动读取。
# 说明：
#   - 本仓库要求 JDK 17（AGP 8.4 / Kotlin 1.9.22 均需要）。
#   - 本地构建通过此文件把 JAVA_HOME 指到本机 JDK 17；
#   - CI（GitHub Actions）无需此文件，build.yml / release.yml 已用 actions/setup-java 指定 JDK 17。
#
# macOS 用如下命令查看本机已安装的 JDK：
#   /usr/libexec/java_home -V
JAVA_HOME=/path/to/your/jdk17
